{
  "name": "@stelolabs/embed",
  "version": "0.0.7",
  "description": "Stelo Embed",
  "main": "dist/main.js",
  "module": "dist/main.js",
  "types": "dist/main.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsc --build ./tsconfig.json --watch",
    "build": "tsc --build ./tsconfig.json",
    "build:iife": "node scripts/build.mjs",
    "prepare": "yarn build && yarn build:iife",
    "clean": "tsc --build ./tsconfig.json  --clean",
    "preview": "vite preview"
  },
  "devDependencies": {
    "jest": "^29.2.2",
    "react": "^17.0.0",
    "typescript": "^4.8.4",
    "vite": "^3.2.2"
  },
  "directories": {
    "dist": "dist"
  },
  "author": "",
  "license": "ISC"
}
